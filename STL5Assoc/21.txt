#include "pt4.h"
using namespace std;
#include <math.h>
#include <iterator>
#include <vector>
#include <set>
#include <map>
#include <functional>
#include <algorithm>

typedef ptin_iterator<int> ptin;

multimap <int,int> M;
void f1(int a)
{
    M.insert(pair<int,int>(abs(a)%10, a ));
}

/*void f2(pair<int,int> p)
{
        pt << p.first;
        pt << p.second;

}
*/
void Solve()
{
    Task("STL5Assoc21");
    vector<int> V(ptin(0), ptin());
    for_each(V.begin(), V.end(), f1);
    
    multimap<int,int>::iterator i;
	for(i=M.begin();i!=M.end();i++)
  {
       pt <<(*i).first <<(*i).second;
  }
    
    //for_each(M.begin(), M.end(), f2);
    //ShowLine(M.begin(), M.end());

}